function BuildDotNet()
{
    docker exec $Repository$Project bash -c "cd /$Repository/$Project && dotnet publish -o /Build"

    sudo docker cp $Repository$Project:/Build /

    docker build -f /PaydarCore/Infra/DotNet/Prod/Dockerfile -t $LowercaseRepo$LowercaseProject /Build
}
